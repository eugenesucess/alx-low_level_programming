// Online C compiler to run C program online
#include <stdio.h>
/**
* print_ array: print array
* return : void

void print_array(int *a, int n)
{
    int i;
    for (i = 0; i <= n; i++)
    {
        printf("%d, ", a[i]);
    }
}
