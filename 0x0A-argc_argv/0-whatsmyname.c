#include <stdio.h>

/**
 * main -< entry point
 * @argc: value of count
 * @argv: array of lines from command
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
